/**
 * Snap Sidebar Cart Frontend Scripts
 */

(function($) {
    'use strict';

    var SnapSidebarCart = {
        init: function() {
            console.log('Snap Sidebar Cart: Frontend scripts loaded');
        }
    };

    // Initialize on document ready
    $(document).ready(function() {
        SnapSidebarCart.init();
    });

})(jQuery);