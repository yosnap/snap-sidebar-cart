# Archivos JavaScript Obsoletos

Esta carpeta contiene archivos JavaScript que ya no se utilizan en el plugin Snap Sidebar Cart.
Se mantienen como referencia histórica, pero no se cargan en el frontend.

Los archivos activos se encuentran en la carpeta principal:
- snap-sidebar-cart-public.js
- snap-sidebar-cart-core.js
- snap-sidebar-cart-ajax.js
- quantity-handler-unified.js